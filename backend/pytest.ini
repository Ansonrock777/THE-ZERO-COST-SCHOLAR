[pytest]
markers =
    integration: marks tests that exercise supplied real-world artifacts locally
filterwarnings =
    ignore:`langchain-community` is being sunset and is no longer actively maintained\..*:DeprecationWarning
